/** @type {import('next').NextConfig} */
const nextConfig = {
        images: {
            remotePatterns: [
                {
                    protocol: 'https',
                    hostname: 'zfhzievlidjjcakykpnb.supabase.co',
                },
            ],
        },

};

export default nextConfig;
